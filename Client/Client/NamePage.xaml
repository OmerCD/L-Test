<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:progress="clr-namespace:Client.M_CustomControls"
             Title="İsmini Girin"
             x:Class="Client.NamePage" BackgroundColor="SteelBlue">
    <Grid>
        <StackLayout HorizontalOptions="Center" VerticalOptions="Center" WidthRequest="300">
            <Label x:Name="Info" TextColor="LightGray"/>
            <Frame Style="{StaticResource EntryBorder}">
                <Entry x:Name="Name" Placeholder="Adınızı Girin" />
            </Frame>
            <Button Text="Onayla" x:Name="IsimGiris" Clicked="SendName_Clicked" Style="{StaticResource ButtonStyle}"/>
        </StackLayout>
        <Frame Style="{StaticResource EntryBorder}" VerticalOptions="End" BackgroundColor="LightSteelBlue" Margin="0">
            <Label Text="Ekranda adınızın göründüğünden emin olun." TextColor="White" FontSize="Medium" HorizontalOptions="Center" VerticalOptions="Center"/>
        </Frame>
    </Grid>
</ContentPage>